import { render } from '@testing-library/react';
test('renders service dashboard', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <ServiceDashboardComponent />
        //     </UserContext>
        // },100)
    );
});