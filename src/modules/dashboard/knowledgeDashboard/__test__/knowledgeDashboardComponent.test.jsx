import { render } from '@testing-library/react';
test('renders Knowledge dashboard', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <ServiceDashboardComponent />
        //     </UserContext>
        // },100)
    );
});