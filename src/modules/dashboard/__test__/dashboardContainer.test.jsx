import { render } from '@testing-library/react';
test('renders dashboard', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <DashboardContainer />
        //     </UserContext>
        // },100)
    );
});