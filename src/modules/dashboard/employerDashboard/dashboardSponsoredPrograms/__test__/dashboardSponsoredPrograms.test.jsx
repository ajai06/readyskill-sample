import { render } from '@testing-library/react';
test('renders dashboard sponsored programes', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <DashboardSponsoredPrograms />
        //     </UserContext>
        // },100)
    );
});