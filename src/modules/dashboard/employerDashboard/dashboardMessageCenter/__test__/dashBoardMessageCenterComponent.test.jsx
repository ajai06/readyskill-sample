import { render } from '@testing-library/react';
test('renders dashboard message center', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <DashboardMessageCenterComponent />
        //     </UserContext>
        // },100)
    );
});