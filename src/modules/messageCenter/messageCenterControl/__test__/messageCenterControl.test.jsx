import { render } from '@testing-library/react';
test('renders message center control', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <MessageCenterControl />
        //     </UserContext>
        // },100)
    );
});