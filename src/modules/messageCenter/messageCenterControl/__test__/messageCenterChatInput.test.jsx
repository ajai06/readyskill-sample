import { render } from '@testing-library/react';
test('renders chat input', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <MessageCenterChatInput />
        //     </UserContext>
        // },100)
    );
});