import { render } from '@testing-library/react';
test('renders message center block', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <MessageCenterMessageBlock />
        //     </UserContext>
        // },100)
    );
});