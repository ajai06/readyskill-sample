import { render } from '@testing-library/react';
test('renders chat window', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <MessageCenterChatWindow />
        //     </UserContext>
        // },100)
    );
});