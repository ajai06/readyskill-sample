import { render } from '@testing-library/react';
import ExternalConnectionForm from '../externalConnectionForm';
test('External connection Form', () => {
    //Mock data
    render(
        <ExternalConnectionForm/>
      );
  });