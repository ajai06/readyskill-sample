import { render } from '@testing-library/react';

test('renders edit groups modal', () => {
    //Mock data
    render(
        // setTimeout(() => {
        //     <UserContext>
        //         <AdminAlertsComponent />
        //     </UserContext>
        // },100)
    );
});