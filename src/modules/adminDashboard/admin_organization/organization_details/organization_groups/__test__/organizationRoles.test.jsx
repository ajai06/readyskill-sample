import { render } from '@testing-library/react';
import OrganizationRoles from '../organizationRoles';
test('renders organization roles', () => {
    //Mock data
    render(
        <OrganizationRoles/>
      );
  });