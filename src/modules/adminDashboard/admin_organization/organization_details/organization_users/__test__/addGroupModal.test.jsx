import { render } from '@testing-library/react';
import AddGroupModal from '../addGroupModal'
test('renders add group modal', () => {
    //Mock data
    render(
        <AddGroupModal/>
      );
  });